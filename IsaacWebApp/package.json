{
  "name": "angular-starter",
  "private": true,
  "version": "0.0.0",
  "description": "A simple starter kit for Angular beginners",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server ./app -a localhost -p 8000 -c-1"
  }
}
